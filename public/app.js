const quizBtn = document.getElementsByTagName('button')[0];

console.log('hello')

quizBtn.addEventListener('click',(ev)=>{
    // PROMPT: use AXIOS/FETCH/jQuery/etc to get data from your server
    console.log('this should send a request to your server');
})